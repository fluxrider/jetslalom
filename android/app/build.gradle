apply plugin: 'com.android.application'

android {
  namespace = "fluxrider.jetslalom"
  compileSdkVersion = "android-36.1"
  defaultConfig {
    minSdkVersion = 36
    targetSdkVersion = 36
    versionCode = 1
    versionName = "1.0"
  }
  buildTypes {
    release {
      minifyEnabled = false
    }
  }
  lint {
    abortOnError = false
  }
}